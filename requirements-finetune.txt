# Fine-tuning dependencies — separate from Karl's runtime requirements.
# Install these only when you want to fine-tune, on a machine with an NVIDIA GPU.
#
# Install with:
#   pip install -r requirements-finetune.txt
#
# NOTE: Unsloth requires CUDA 11.8+ and a compatible torch build.
# If the line below fails, follow the manual install at https://github.com/unslothai/unsloth

unsloth[colab-new]
trl>=0.8.6
datasets>=2.18.0
transformers>=4.40.0
accelerate>=0.28.0
bitsandbytes>=0.43.0
