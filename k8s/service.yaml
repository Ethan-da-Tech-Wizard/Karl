apiVersion: v1
kind: Service
metadata:
  name: karl-backend
  labels:
    app.kubernetes.io/name: karl
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: karl
    app.kubernetes.io/component: backend
  ports:
    - name: wss
      port: 8080
      targetPort: wss
      protocol: TCP
