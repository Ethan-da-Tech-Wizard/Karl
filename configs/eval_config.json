{
  "_comment": "Karl Eval Config. Run: python -m karl_finetune.run_eval configs/eval_config.json --mode base",

  "model_name":  "TinyLlama/TinyLlama-1.1B-Chat-v1.0",
  "adapter_dir": "outputs/adapters/karl_lora",
  "eval_file":   "data/eval.jsonl",
  "reports_dir": "outputs/reports",
  "template":    "alpaca",

  "max_new_tokens": 256,

  "_notes": [
    "Run with --mode base to test the base model first",
    "Run with --mode tuned to test after training",
    "Then run compare_outputs to get the report"
  ]
}
